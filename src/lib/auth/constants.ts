/**
 * Auth constants that are safe to import from any runtime — client, server,
 * proxy/middleware. Do not put anything that touches `next/headers` here.
 */

export const AUTH_COOKIE_NAME = "darso_account";
